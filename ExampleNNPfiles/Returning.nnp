
asciiToCh! <- 10 -> \n

reter <-
    speak! <- "Function Run"
    "RETURN!" ->
    speak! <- "Do not print!"
no





stringToAscii <- str

    [ ] -> retList
    0 -> index
    str strLen! <- -> length

    loop index < length
        0 -> i
        loop i < 256

            asciiToCh! <- i -> ch
            str substring! <- index index -> s_ch
            if s_ch = ch
                retList + i -> retList
                loop ->
            no
            
            i + 1 -> i
        no

        index + 1 -> index
    no
    retList ->
no





stringToAscii <- listen! <- -> l


speak! <- l ~-> StrObj + \n












->

charToAscii <- s_ch
    0 -> i
    loop i < 256

        asciiToCh! <- i -> ch
        
        if s_ch = ch
            i ->
        no
        
        i + 1 -> i
    no
    -1 ->
no



stringToAscii2 <- str fn

    [ ] -> retList
    0 -> index
    str strLen! <- -> length

    loop index < length
        
        str substring! <- index index -> s_ch
        fn <- s_ch -> ch

        retList + ch -> retList
        index + 1 -> index
    no
    retList ->
no
